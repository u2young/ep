# 薪酬管理模块冒烟测试: 独立内存 H2, 扫描 pay+hr+fin(员工 REF + 工资 GL 过账集成)
spring:
  datasource:
    url: jdbc:h2:mem:pay-test;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=FALSE;NON_KEYWORDS=VALUE,VALUE_
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

erupt:
  app-name: Pay Test

logging:
  level:
    root: WARN
    xyz.erupt: WARN
    org.hibernate.SQL: WARN
