# erupt-ai-project 项目上下文

## 你必须知道的事（每次交互前必读）

### 环境要求
- **必须用 Java 17**，系统默认 Java 26 不兼容。编译/测试前执行：
  ```bash
  export JAVA_HOME=/opt/homebrew/Cellar/openjdk@17/17.0.19/libexec/openjdk.jdk/Contents/Home
  export PATH="$JAVA_HOME/bin:/opt/homebrew/bin:$PATH"
  ```
- Maven 路径: `/opt/homebrew/bin/mvn`

### 项目概况
- Spring Boot 3.5.15 + Erupt 2.0.1 + H2 嵌入式数据库
- 唯一业务实体: `SimpleEntity`（表名 demo_simple），5个字段
- 16 个 erupt 模块已集成（admin, web, ai, terminal, tpl, job, generator, monitor, magic-api, excel, security, designer, websocket, notice, print）
- 测试全部通过（20个），启动类使用 WebApplicationType.SERVLET

### 代码约定
- 包名: `com.example.eruptaiproject`
- 实体放在 `entity/` 子包
- 测试放在 `test/java/com/example/eruptaiproject/` 对应位置
- 所有 erupt 实体必须加 `@EruptScan` 自动扫描

### 参考文件
- 详细记忆索引: `.agents/memory.md`
- 应用配置: `src/main/resources/application.yml`
